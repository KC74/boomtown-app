export const mainURL = 'http://localhost:3001';

// export const dataOptions = (dataSet) => {
//     return {
//         uri: `${mainURL}/${dataSet}`,
//         json: true // Automatically parses the JSON string in the response
//     }
// };